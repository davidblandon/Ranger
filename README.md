# Microservices Project Scaffold

Workspace overview:

- `frontend/`: React app scaffold (still pending implementation).
- `services/rh-service/`: Spring Boot HR microservice with clean architecture and documented OOP usage.
- `services/production-service/`: Spring Boot Production microservice scaffold.
- `docker/`: Docker and orchestration files.

Current status:

- `services/rh-service/` is no longer a placeholder; it contains the cleaned backend structure, domain models, application ports/services, and persistence adapters.
- `services/production-service/` and `frontend/` remain placeholders for the next phase.

See `services/rh-service/README.md` for the detailed RH-service architecture, data flow, OOP principles, and run instructions.
